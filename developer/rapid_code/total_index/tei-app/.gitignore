node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store

# Secrets — never commit. .env.example is the committed template.
.env
.env.*
!.env.example

# Test + coverage output
coverage/
test-report/
jest-html-reporters-attach/
jest_html_reporters.html

# Build artifacts — APKs and the web zip are distributed, not versioned
builds/
# Recorded walkthroughs / screenshots from QA runs
test-artifacts/

# Native Android build output (the project itself IS committed, below it)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/*.iml
android/.cxx/

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli